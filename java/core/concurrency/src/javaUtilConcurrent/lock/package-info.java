package javaUtilConcurrent.lock;
/**
 * Mechanism for coordinating access to shared data
 * 1. synchronized
 * 2. volatile
 * 3. Reentrant Lock (jdk5+) (provide some advanced feature where intrinsic locking option is limited e.g, interrupt while waiting to acquire a lock
 * 
 *  ReadWriteLock -> ReentrantReadWriteLock
 * 
 */
