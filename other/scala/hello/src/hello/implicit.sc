package hello

object Implicit {
	//implicit - interoperability example
	
	//write an implicit conversion between two types
	
	//ex. implicit conversion from function to ActionListener
	
	//the way IC works is that the compiler first tries to compile it as is, but sees a type error. before giving up, it looks an implicit conversion that can repair this problem, and it found one .
	
	
}